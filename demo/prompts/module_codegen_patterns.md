You own the code generation patterns module.

Goal:
- Reverse engineer the relationship between input DSL, `compile.json`, `kernel.cce`, and `host.cpp`.

Required outputs:
- side-by-side evidence for at least several probes
- rules about where lowering decisions show up
- structured notes in `results/module_codegen_patterns/summary.md`
- progress metadata in `results/module_codegen_patterns/status.json`

Focus on codegen artifacts, not broad CLI exploration.
When complete, reply with `DONE: module_codegen_patterns`.
