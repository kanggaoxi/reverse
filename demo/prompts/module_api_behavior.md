You own the API behavior module.

Goal:
- Reverse engineer which `api:` names are accepted and whether different APIs trigger different lowering behavior.

Required outputs:
- a catalog of passing and failing api names
- examples comparing outputs for different APIs with the same shape
- structured notes in `results/module_api_behavior/summary.md`
- progress metadata in `results/module_api_behavior/status.json`

Keep experiments minimal and evidence-driven.
When complete, reply with `DONE: module_api_behavior`.
